# TODO

Ideen und offene Punkte, die nicht sofort umgesetzt wurden, plus ein Verlauf des bereits Erledigten. Nach Themen gruppiert (innerhalb von "Erledigt" jeweils neueste zuerst).

## Offen

### Tests, Qualität & Audits
- [ ] **Automatisierte Tests: `analyzeRoute()`/Training Decision Engine abdecken** — Streckengenerierung/Intervall-Platzierung in `analyzeRoute()` (Kandidaten: `smoothElevation`, `computeCurvature`, `detectIntersectionCandidates`, `placeIntervalsOnRoute` u. a.) sowie die Training Decision Engine/Adaptive-Learning-Pipeline (`computeTrainingDecisionState`, `evaluateWorkout` u. a.) — beide pure Funktionen, aber mit spürbarem Fixture-Aufwand (synthetische Höhenprofile bzw. mehrwöchige Fahrhistorien). Höchstes verbleibendes Test-Risiko laut Vollaudit (03.09.2026), da hier die komplexeste Entscheidungslogik der App liegt.
- [ ] **Automatisierte Tests: FIT/ZWO-Bauer + GPX-Import abdecken** — die byte-genauen Bauer selbst (`buildFit`/`buildZwo`/`buildCourseFit`) sind noch ungetestet, nur ihre Bausteine (`fitCrc`/`fitString`/`xmlEscape`) wurden im Vollaudit (03.09.2026) abgedeckt. `parseGpxTrack` ist zusätzlich blockiert: das Test-Harness (`test/support/loadApp.js`) hat keinen `DOMParser`-Stub, `new DOMParser().parseFromString(...)` wirft dort aktuell einen `ReferenceError`.
- [ ] **CI/Git-Hook: Tests automatisch vor jedem Push laufen lassen** — laut Nutzer sollen Tests "vor jeder neuen Version durchlaufen", aktuell gibt es dafür keine Automatisierung (kein CI im Repo). Müsste entweder ein manueller Schritt vor jedem Push bleiben oder ein Git-Hook werden (noch nicht eingerichtet, da das eine Änderung am Commit-Workflow ist, die erst abgesprochen werden sollte).

### Routen & Streckengenerierung
- [ ] **Parameter für Streckengenerierung anhand eines Beispiels festlegen** — statt Werte einzeln einzustellen, eine Beispielstrecke angeben und die Streckengenerierungs-Parameter daraus ableiten lassen. Erkenntnis aus der Analyse der Sweet-Spot-Aktivität vom 25.08.: das eigentliche Problem war meist nicht die Steigungs-Einstellung (idealMin/Max), sondern `minIntervalMin` vs. verfügbare zusammenhängende Anstiegslänge — kurze Anstiege (<2km) fallen bei Tempo/Sweet-Spot (minIntervalMin=8) grundsätzlich raus, unabhängig von den Steigungs-Grenzwerten. Eine "aus Beispiel ableiten"-Funktion müsste das mit einrechnen.

### Trainer & Aktivitäten
- [ ] **Top-3-Strecken-Anzeige fürs heutige Workout verbessern** — bestehende Top-Strecken-Karte im Trainer-Tab (renderTopRoutesCard) optisch/inhaltlich aufwerten.

### Segmente
- [ ] **Segmentjäger-Modus** — neue Funktion, Details noch offen.

### Geräte-Integration
- [ ] **IGP Sport BSC200: Workout-Upload per Klick** — VeloCast soll generierte Workouts als Datei für den BSC200 bereitstellen. Keine offene API für Wireless-Push vorhanden; realistischer Weg ist eine generierte Workout-Datei für den `Workouts`-Ordner des Geräts (USB-Massenspeicher-Modus). Offen: exaktes Dateiformat, das iGPSPORT im `Workouts`-Ordner erwartet, muss noch verifiziert werden (offizielle Doku dünn, evtl. Reverse-Engineering einer Beispieldatei nötig).
- [ ] **Workout-Export zu Head-Unit (Wahoo/Garmin)** — Idee (01.09.2026). Analog zum bestehenden Hammerhead-`.fit`-Upload-Proxy im Worker (`worker/src/index.js`) auf weitere Geräte-Ökosysteme erweitern.

### Neue Ideen (noch nicht entschieden)
- [ ] **Vorhersage-basierte Fahrplanung** — Idee (01.09.2026, aus Feature-Session, noch nicht abgesprochen). Für eine gespeicherte Strecke die kommenden Tage auf Wind-/Regen-Vorhersage prüfen (Open-Meteo-Anbindung existiert bereits für den aktuellen Tag) und den günstigsten Zeitpunkt vorschlagen.
- [ ] **Jahres-Rückblick / Saison-Stats** — Idee (01.09.2026). Aggregierte Rückblicksseite (km, Höhenmeter, PRs, stärkste Segmente, genutztes Potential über die Saison) aus bereits vorhandenen Daten, ohne neue API-Aufrufe.
- [ ] **Segment-Sammlungen/Tags** — Idee (01.09.2026). Eigene Gruppen für markierte Segmente (z. B. "Feierabendrunde", "Bergtraining") zum Filtern von Liste und der Übersichtskarte — wird mit wachsender Zahl markierter Segmente relevanter.
- [ ] **Bikefitting-Modul** — Idee (03.09.2026), Details noch offen.

## Erledigt

### Design & UI
- [x] **Mobil/Desktop-Layout differenziert** — umgesetzt (03.09.2026, Nachtrag zum Vollaudit). Neue `@media (max-width:620px)`-Regeln (gleicher Cutoff wie der bereits bestehende `.settings-row`-Breakpoint, kein neuer willkürlicher Wert) verdichten `.app`- (28/20/60px → 20/14/40px), `.card`- (22px → 16px Padding, 16px → 12px margin-bottom) und `.page-title`-Abstände auf schmalen Bildschirmen, während Desktop komplett unverändert bleibt. `.brand`s sticky-Header-Offset (das negative-margin/padding-Duplikat von `.app`s Padding) wurde synchron mitgezogen, sonst wäre der Header bei <620px minimal versetzt gewesen. Bewusst NICHT angefasst: Typografie-Skala — im Vollaudit nicht als Lesbarkeitsproblem geflaggt, nur als Dichte-Frage, und kleinere Schrift birgt mehr Regressionsrisiko als reduzierte Abstände. Ein echter Cascade-Bug dabei selbst gefunden und behoben: die erste Fassung platzierte die `.card`/`.page-title`-Mobil-Regel VOR deren Basis-Regeln im Stylesheet — bei gleicher Selektor-Spezifität gewinnt die später im Quelltext stehende Regel unabhängig von der Media-Query-Bedingung, wodurch die Basis-Regel (22px) die Mobil-Regel (16px) auf einem echten Handy stillschweigend überschrieben hätte. Gefunden durch `getComputedStyle()`-Prüfung bei 375px, behoben durch Verschieben der Mobil-Regel hinter die Basis-Regeln (wie `.app`s bereits vorhandene Overrides es schon richtig machten). Verifiziert: 229/229 Tests grün, `.app`/`.card`/`.brand`-Padding bei 375px UND 1440px per `getComputedStyle()` geprüft (korrekt unterschiedlich bzw. korrekt identisch), Sticky-Header-Ausrichtung bei 375px visuell per Screenshot bestätigt.
- [x] **Doppelte Toggle-Switch-Implementierung konsolidiert** — umgesetzt (03.09.2026, Nachtrag zum Vollaudit). `renderToggleSwitch()` baut jetzt intern dieselbe zugängliche `<input type=checkbox>` + `.tsettings-switch`/`.tsettings-slider`-Markup wie die Trainer-Einstellungen (`settingsFieldRow()`) statt eines reinen `<div onclick>`-Knopfs — ein echtes, tastatur-fokussierbares Formularelement mit echtem `:checked`-Status statt einer visuellen Attrappe. Die ganze Zeile (Schalter + Text) ist jetzt EIN äußeres `<label>`, sodass ein Klick überall darauf (Schalter oder Text) die verschachtelte Checkbox nativ umschaltet — kein manuelles `onclick` mehr nötig, kein Doppel-Feuern-Risiko, verschachtelte interaktive Elemente wie `infoButton()` (die selbst `stopPropagation()` aufrufen) bleiben unberührt. Alle 8 bestehenden Aufrufer (Windfeld-Schalter jetzt inklusive, vorher eine eigenständige Kopie) profitieren automatisch, keine Signaturänderung nötig — alle bestehenden `onToggle`-Callbacks lasen ohnehin nie ihr Argument, sondern immer den aktuellen `state` direkt. Die drei Trainer-Einstellungen-Schalter mit eigener `id`/`<label for>`-Bindung (`settingsFieldRow()`, `settingsFieldChip()`, LTHR-Quelle-Umschalter) blieben unverändert, da sie schon das richtige Muster nutzten und eine andere Form (id-gebunden statt Callback-basiert) brauchen. Verifiziert: 229/229 Tests grün, `renderToggleSwitch()`-Ausgabe visuell in einer isolierten Testseite mit der echten `.tsettings-switch`-CSS geprüft (an/aus-Zustand, Knopf-Position korrekt).
- [x] **Hamburger-Menü: Struktur + fixierter Header** — umgesetzt (01.09.2026, via Worker-Supervisor). Neue Reihenfolge: Strecke, Meine Segmente, Profil, Aktivitäten, Trainer, Reifendruck, Änderungen, Trennlinie, dann Einstellungen zusammen mit den Strava-Aktionen — Einstellungen bleibt dabei bewusst auch ohne Strava-Verbindung erreichbar (nicht in den `if(state.connected)`-Block gerutscht). Header mit Hamburger-Button ist jetzt `position:sticky`, bleibt beim Scrollen auf langen Seiten sichtbar/erreichbar.

### Karte
- [x] **Wind-Pfeil-Icons auf der Karte vereinheitlicht** — umgesetzt (03.09.2026, Nachtrag zum Vollaudit). Neue gemeinsame Funktion `mapDirectionArrowSvg({size, rotateDeg, color, withDot, filled, strokeWidth, dropShadow})` ersetzt die drei unabhängig geschriebenen Inline-SVG-Strings (Segment-Wind-Marker, Windfeld-Overlay-Pfeil, Routen-Richtungs-Chevron) durch eine Zeichenfunktion mit expliziten Parametern statt drei Ad-hoc-Kopien mit leicht unterschiedlichen, von Hand gewählten Linien-/Chevron-Koordinaten. Wind-Icons (Umriss aus Linie+Chevron, optionaler Mittelpunkt) und das reine Fahrtrichtungs-Icon (gefüllter, neutraler Pfeilkopf) bleiben bewusst visuell unterscheidbar — sie bedeuten unterschiedliche Dinge (Windrichtung vs. Fahrtrichtung) — jetzt aber als eine Zeichenfunktion statt drei. `fill`/`stroke` laufen jetzt über `style="..."` statt nackte Attribute, damit eine CSS-Variable als Farbe (der Routen-Chevron nutzt jetzt `var(--chalk)` statt hartkodiertem `#f2efe9`) zuverlässig aufgelöst wird, sobald Leaflet das Markup ins echte DOM einfügt. Verifiziert: 229/229 Tests grün, alle drei Icon-Varianten aus der echten Funktion extrahiert und visuell in einer isolierten Testseite geprüft (Rotation, Punkt/kein-Punkt, gefüllt/Umriss korrekt).
- [x] **Karte: Sichtbarkeit + Menü-Überlagerung behoben** — umgesetzt (01.09.2026). Segment-Übersichtskarte überlagerte das offene Hamburger-Menü (`#route-map` hatte keinen eigenen Stacking-Context, Leaflets interne Panes mit z-index bis 1000 konkurrierten direkt in der Seiten-Ebene statt im Menü "gefangen" zu bleiben) — behoben durch `position:relative; z-index:0` auf `#route-map`. Segment-Linien (`#82a2d1`) waren gegen OSM-Kachelfarben schlecht sichtbar — dunkle Kontur-Linie (`#333a47`, bereits an anderer Stelle als Basisfarbe genutzt) darunter ergänzt, keine neue Farbe.
- [x] **Kartenkacheln repariert (CARTO → OpenStreetMap)** — umgesetzt (28.08.2026). CARTO hat sein bisher kostenloses, key-loses Raster-Tile-Angebot (`basemaps.cartocdn.com`) eingestellt — jede Kachel zeigte nur noch "API KEY REQUIRED". Einen CARTO-Key zu besorgen bräuchte eine Kontoregistrierung, die ich nicht in Lucas Namen anlege; stattdessen läuft die Karte jetzt auf Standard-OpenStreetMap-Tiles (`tile.openstreetmap.org`), echt kostenlos und ohne Key/Anmeldung. Nebeneffekt: der Windfeld-Farbverlauf (`--wind-*`) war ursprünglich mit gemessenen Kontrastwerten gegen CARTOs einheitlichen hellen Hintergrund (#f2f0eb) abgestimmt; OSM-Kacheln haben keinen einheitlichen Hintergrund mehr (Wald grün, Wasser blau, etc.), Kommentar dazu aktualisiert (`index.html:~79`) — Farben sind in der Praxis weiterhin gut lesbar, aber nicht mehr rechnerisch für jede Kachelfarbe garantiert.

### Tests, Qualität & Audits
- [x] **Vollaudit der App (Tests, Logik, Totencode, Design)** — durchgeführt (03.09.2026, 4 parallele Recherche-Agenten für Test-Abdeckung/Logik-Fehler/Totencode/Design-Konsistenz, danach sichere Fixes direkt umgesetzt). Ergebnis in Kurzform: **Totencode** — keiner gefunden, alle 443 Top-Level-Funktionen, alle CSS-Klassen und alle localStorage-Keys haben mindestens eine echte Referenz. **Logik-Fehler** — zwei echte gefunden und behoben: (1) Fünf Nutzer-sichtbare Labels ("42-Tage-Bestwert" im FTP-Chart/Metric-Card/Legende/Export-Beschreibung, `index.html` ~3437/6620/6893/6905/6911) waren seit der Umstellung von `CEILING_WINDOW_DAYS` von 42 auf 365 Tage (siehe "Potential-Feature nachgebessert" unten) nicht mehr aktualisiert worden — zeigten weiterhin "42-Tage"/"6 Wochen", obwohl die zugrunde liegende Berechnung längst ein Jahres-Fenster nutzt; ein Fahrer hätte einen 11 Monate alten Bestwert für einen frischen halten können. Jetzt dynamisch über `CEILING_WINDOW_DAYS` gebaut, nicht mehr hartkodiert. (2) `pickRoutePoints()` hatte einen hartkodierten `Math.max(1, stepKm)`-Boden, der den von `computeWindFieldRoutePoints()` bewusst gesetzten `WIND_FIELD_MIN_STEP_KM=0.5` auf kurzen Strecken (<~16km) stillschweigend auf 1km aufrundete — das Windfeld auf kurzen Strecken hatte dadurch nur die halbe beabsichtigte Pfeil-Dichte. Boden auf 0.05 (reine Div-durch-0-Absicherung) gesenkt, die beiden anderen Aufrufer (`routePointDistanceKm`≥1, `LIST_ITEM_WIND_STEP_KM=1`) bleiben unverändert, da sie nie unter 1km anfragen. **Design-Konsistenz** — insgesamt überdurchschnittlich konsistent (Token-System, Chip-/Karten-/Banner-Muster werden auch von den neuesten Features eingehalten). Drei kleine, sichere Fixes direkt umgesetzt: zwei hartkodierte `#fff`-Toggle-Knöpfe durch das bestehende `var(--chalk)`-Token ersetzt; die Priority-Badge (Trainer-Tab) nutzte einen eigenen `border-radius:10px` statt des etablierten Pill-Radius — jetzt `var(--r-full)`, wie `.cpc-badge`; drei Trainer-Tab-Mikro-Labels fehlte `font-weight`/`letter-spacing` gegenüber dem sonst überall genutzten Uppercase-Label-Rezept (`.stat-chip .l`/`.metric-card .ml`) — ergänzt. **Nicht direkt angefasst** (größerer Umbau statt eines sicheren Fixes, daher als eigene Einträge geplant statt spontan mitgemacht) — beide inzwischen umgesetzt, siehe "Doppelte Toggle-Switch-Implementierung konsolidiert" und "Wind-Pfeil-Icons auf der Karte vereinheitlicht" oben: doppelte Toggle-Switch-Implementierung, uneinheitliche Wind-Pfeil-Icons auf der Karte. **Test-Abdeckung** — siehe eigener Eintrag unten. Alle 229 Tests grün nach den Fixes, `index.html` lädt weiterhin sauber (`node --test`/Syntax-Check verifiziert).
- [x] **Automatisierte Tests: Abdeckung erweitert (erste Runde von der Vollaudit-Lücke)** — umgesetzt (03.09.2026, im Rahmen eines Vollaudits der App). `test/support/loadApp.js` bekam einen `initialLocalStorage`-Parameter (localStorage vor dem Skript-Start vorbefüllen — nötig, weil `state`/`savedSettings` beim Top-Level-Skript-Lauf einmalig aus `localStorage` gelesen werden, ein `setItem()` danach kommt zu spät). Neue Dateien mit 100 zusätzlichen Tests: `test/geometry.test.js` (`decodePolyline`, `haversineKm`, `buildCumulativeDistances`, `estimateArrival`, `indexAtDistance` — Routen-Geometrie-Grundbausteine für so gut wie jedes Feature), `test/export-helpers.test.js` (`xmlEscape`, `fitString`, `slugify`, `fitCrc` — letzteres gegen eine unabhängig geschriebene CRC-16/ARC-Referenzimplementierung verifiziert, nicht nur gegen sich selbst), `test/date-helpers.test.js` (`parseIsoDateUTC`/`toIsoDate`/`addDaysUTC`/`mondayOfUTC`, inkl. Wochen-/Monats-/Jahres-/Schaltjahr-Grenzfällen), `test/training-decisions.test.js` (`weightedLinearRegression`, `computeTestDueSignal`), `test/settings-migration.test.js` (genau die zuvor namentlich offene `autoLoadOnVisit`→3-Flags-Migration, inkl. korruptem JSON und explizitem `false`-Erhalt). 229/229 Tests grün, `index.html` durch die Test-Ergänzung selbst unverändert. Noch offene Testlücken und die CI/Git-Hook-Frage sind jetzt eigene Einträge oben unter "Tests, Qualität & Audits" (Offen).
- [x] **Automatisierte Tests: Grundgerüst + erste Funktionen** — umgesetzt (29.08.2026, via Worker-Supervisor, 2 Runden). Erstes Test-Setup überhaupt: `package.json` (nur `"scripts":{"test":"node --test test/*.test.js"}`, keine neuen Abhängigkeiten, kein Build-Step für die App selbst) + `test/support/loadApp.js` — ein Node-`vm`-Loader, der das einzelne `<script>` aus `index.html` extrahiert und in einer sandboxten Umgebung ausführt (permissiver Proxy-DOM-Stub + gezielte Stubs für `localStorage`/`fetch`/`navigator` usw.), damit auch das am Skript-Ende selbst aufgerufene `init()` gefahrlos läuft — nachweislich ohne echte Netzwerkzugriffe (eigener Test dafür: `loadApp() safety`). Frischer vm-Kontext pro Aufruf, `state` leckt also nie zwischen Tests. 40 Tests in `test/pure-functions.test.js` für die 6 reinen, DOM-/netzwerkfreien Funktionen aus dem ursprünglichen TODO-Eintrag (`windRelative`, `bearingBetween`, `smoothedBearingAt`, `buildPotentialChip`, `estimatePowerCurveAt`, `computeFtpAnalysis`), inkl. gezielter Grenzfälle (Spitzkehren-Fallback, >100%-Potential unkappiert, rollierendes FTP-Ceiling-Fenster inkl. Verfall alter Peaks außerhalb `CEILING_WINDOW_DAYS`) — Runde 1 des Supervisor-Reviews fand 3 echte Lücken (Ceiling-Fenster-Logik faktisch ungetestet, eine Bearing-Assertion nur auf Wertebereich statt korrektem Wert geprüft, der Netzwerk-Sicherheits-Check selbst ungenutzt), Runde 2 behob alle drei plus 2 optionale Verbesserungen und wurde freigegeben. `index.html` selbst unverändert (`git diff index.html` leer) — rein additiv. Ausführen: `npm test` im Projektverzeichnis.

### Routen & Streckengenerierung
- [x] **Anstiegs-Unterbrechungs-Toleranz in `analyzeRoute()`** — umgesetzt und mit echten Strava-Daten verifiziert (Aktivität "Sweet Spot intervals", 25.08.2026). Neue Einstellungen unter "Allgemeine Strecken-Constraints": "Anstiegs-Unterbrechung: Max. Länge" (`maxInterruptionM`, Default 300m) und "Anstiegs-Unterbrechung: Max. Gefälle" (`maxInterruptionDescentPct`, Default -1.5%) — eine kurze, nicht wirklich abfallende Lücke zwischen zwei Anstiegs-Segmenten wird jetzt als ein durchgehender Anstieg gewertet statt in zu kurze Stücke zerteilt zu werden. Verifiziert: aus 587m/292m-Fragmenten wurde ein durchgehender 1,47km-Anstieg; für einen tatsächlich platzierten Sweet-Spot-Intervall auf dieser Strecke musste zusätzlich noch "Schwelle: Anstieg" (`climbGradientThresholdPct`, Streckengenerierung) von 2.5% auf 2.0% gesenkt werden — dann landete ein echtes 4km-Intervall auf dem Anstieg. Beide Stellschrauben sind bereits vorhandene Einstellungen.

### Strecke & Wetter
- [x] **5 gemeldete Bugs behoben** — umgesetzt (28.08.2026, via Worker-Supervisor, 2 Runden). (1) Datum/Uhrzeit-Eingabe auf Desktop: fehlendes `color-scheme: dark` machte das Kalender-Symbol des nativen `datetime-local`-Inputs praktisch unsichtbar — global auf `:root` ergänzt. (2) Windklassifizierung wirkte zufällig: `pickRoutePoints()` berechnete das Bearing jedes Streckenpunkts aus nur einem Paar direkt benachbarter Roh-GPS-Punkte — schon kleines GPS-Rauschen konnte das Bearing um Dutzende Grad kippen und die Einordnung für ganze ~10km-Abschnitte falsch setzen. Jetzt gemittelt über ein Distanzfenster (`smoothedBearingAt()`, mit Fallback bei Kehrtwenden/Spitzkehren, wo das Fenster sonst entartet). Derselbe Fix auch im Wind-Chip der Routen-/Aktivitätenliste (`ensureListItemWindTag()`, vorher noch ungefilterter als die Kartenansicht), dort zusätzlich auf Bearing über die volle ~1km-Beinlänge statt nur um den Startpunkt umgestellt. `windRelative()`-Schwellenwerte (55°/125°) unverändert, waren bereits korrekt. (3) Neue Einstellung "Daten beim Besuch automatisch laden" (Einstellungen-Seite, Default: an — bestehendes Verhalten bleibt für alle unverändert, bis jemand aktiv abschaltet). Aus: zeigt einen "Daten laden"-Button statt automatisch zu laden. (4) Start-Ladevorgang zeigt jetzt eine Schritt-für-Schritt-Fortschrittsanzeige (Token → Aktivitäten → erste Route/Wetter → Segmente) statt nur wechselnder Text-Labels — neue, aber bewusst minimale CSS-Komponente (keine Keyframes), betrifft nur den Start-Ladevorgang, nicht die ~11 anderen bestehenden Lade-Anzeigen in der App. (5) Desktop-Breite: der feste `max-width: 1080px`-Deckel ab 1200px Fensterbreite wurde entfernt (`max-width: none` + mit der Fensterbreite mitwachsendes Padding) — auf breiten/ultrawide Monitoren wird jetzt die volle Breite genutzt, Mobil-Ansicht unverändert. Supervisor-Runde 1 fand 2 echte Bugs (Fortschrittsbalken beim Token-Refresh-Schritt unsichtbar; der bestehende "Neu laden"-Link in der Fußzeile umging das Zurücksetzen von `needsManualLoad` und ließ Nutzer auf der manuellen Lade-Karte hängen), Runde 2 behob beide und wurde freigegeben.
- [x] **Wind: erste vs. zweite Streckenhälfte** — umgesetzt (28.08.2026, via Worker-Supervisor, 2 Runden), Darstellung danach nochmal überarbeitet. Neue Aufteilung der bestehenden Rückenwind/Seitenwind/Gegenwind-Mischung nach Streckendistanz: `computeWindMixForRange(fromKm, toKm)` (verallgemeinert die bisherige `computeWindMix()`, exakt verhaltensgleich verifiziert) plus `computeWindMixHalves()` für den Split bei der halben Streckendistanz. Bei kurzen Strecken (≤ eingestellter Punktabstand, nur 1 Segment) wird der Block bewusst ganz weggelassen statt eine Hälfte falsch zu beschriften. Die erste Version zeigte für jede Hälfte einen vollen dreifachen Hero-Stats-Block (wirkte wie 3x dieselbe Grafik übereinander) — auf Nutzer-Feedback hin ersetzt durch eine kompakte Variante: pro Hälfte nur Label + eine Zahl (jeweils die dominante Windart) + ein schmaler Balken, beide Hälften nebeneinander, gleiche `.wind-mix-bar`-Optik wie die Gesamt-Kennzahl direkt darüber.
- [x] **Datum/Uhrzeit-Eingabe auf Desktop springt sofort weiter** — behoben (29.08.2026). Der vorherige Fix (`color-scheme: dark`) hatte nur die Sichtbarkeit des Kalender-Symbols behoben, nicht die eigentliche Eingabe: `startTimeInput`s `onchange` löste bei jeder Änderung sofort `loadRouteWeather()` + `render()` aus, aber der Browser feuert `change` pro Segment (Stunde, dann Minute), nicht erst beim fertigen Wert — der Seiten-Neuaufbau mitten in der Eingabe blur(t)e das Feld und setzte es zurück, sodass sich z.B. „15" nicht eintippen ließ (nur die „1" kam an, dann Sprung ins nächste Feld). Fix: `onchange` debounced jetzt um 600ms, sodass schnell aufeinanderfolgende Segment-Changes zu genau einem verzögerten Update mit dem finalen Wert kollabieren, statt bei jedem Tastendruck sofort neu zu rendern.

### Segmente
- [x] **Meine Segmente: Sortierung nach Potential + KOM/QOM-Nähe** — umgesetzt (01.09.2026, via Worker-Supervisor, 2 Runden). Neue Sortieroption „Bestes Potential zuerst" (nutzt dieselbe Berechnung wie der „Genutztes Potential"-Chip, `computePotentialPercent()` extrahiert); Segmente ohne genug Daten dafür sortieren ans Ende statt einen Fehler zu werfen. Außerdem eine echte Lücke geschlossen: KOM/QOM-Sortierung existierte bereits in der Sortier-Logik und beim abgeschlossenen-Segmente-Dropdown, war aber nie ins „Meine Segmente"-Dropdown selbst eingebunden — jetzt nachgeholt, unabhängig synchronisiert mit den bestehenden KOM/QOM-Anzeige-Schaltern.
- [x] **Meine Segmente: Übersichtskarte aller markierten Segmente** — umgesetzt (01.09.2026, via Worker-Supervisor). Neue Karte oben auf der Listenansicht zeigt alle markierten Segmente gleichzeitig als Linien (Geometrie war durch das bestehende eager-Laden schon vorhanden, kein neuer API-Call nötig). Bewusst statisch/nicht klickbar (keine Segment-Auswahl über die Karte) — reine räumliche Übersicht. Respektiert den "Nicht relevante Segmente ausblenden"-Filter, nutzt die bestehende persistente Leaflet-Instanz (neuer `"segments-overview"`-Zweig in `currentRouteKey()`), verschwindet komplett statt leerer Karte, wenn keine markierten Segmente mit Geometrie vorhanden sind.
- [x] **Segmente: KOM/QOM einzeln ein-/ausblendbar** — umgesetzt (01.09.2026, via Worker-Supervisor). Zwei neue Schalter in Einstellungen ("KOM anzeigen"/"QOM anzeigen", Default an), wirken auf beide Chip-Quellen (Sterne-Segmente und abgeschlossene Segmente) sowie die Sortier-Dropdown-Optionen (mit Fallback auf Standard-Sortierung, falls gerade aktiv). Bewusst nur für KOM/QOM, nicht für andere Segment-Werte (PR, Potential, Höhenmeter, Anzahl Befahrungen) — die sind entweder die eigene Leistung oder neutrale Fakten, keine "fremden Bestzeiten", die je nach Geschlecht unterschiedlich relevant sind. Kein Geschlechts-Feld/Auto-Erkennung eingebaut (existiert nirgends im Profil) — bewusst einfache manuelle Schalter statt automatischer Ableitung.
- [x] **"Genutztes Potential" für Segmente** — umgesetzt (27.08.2026). Neue Kennzahl auf beiden Segment-Ansichten (Sterne-Segmente und auf einer Route abgeschlossene Segmente): Segment-Ø-Watt als % der auf die gleiche Dauer log-log-interpolierten MMP-Bestleistung (`estimatePowerCurveAt`, `buildPotentialChip`). Fehlt die MMP-Kurve (Trainingsdaten nicht geladen), wird der Chip still weggelassen, kein Hinweis, kein Auto-Fetch. Farbkodierung spiegelt die bestehenden KOM/QOM-Nähe-Schwellen (≥95%/≥85%).
- [x] **Potential-Feature nachgebessert** — umgesetzt (27.08.2026). Vier Fixes: (1) Ruckeln beim Start behoben — `loadSegmentExtras()` rendert nur noch einmal am Ende statt pro Segment. (2) Potential ist jetzt sofort in der Segmentliste sichtbar statt erst nach Klick — `prWatts` wird für Sterne-Segmente eager mitgeladen (`fetchSegmentPrWatts()`, parallel zu Wetter/Detail). (3) Referenzzeitraum für Potential und FTP von 90/42 auf einheitlich 365 Tage verlängert (`CEILING_WINDOW_DAYS`), Test-Fälligkeits-Anzeige im Trainer-Tab bleibt bewusst bei eigenständigen 42 Tagen (`TEST_STALENESS_DAYS`, jetzt entkoppelt). (4) Farben vereinheitlicht — Potential-Chip nutzt jetzt die App-weite Grün/Gelb-Ampel (`--tail`/`--cross`) statt der KOM/QOM-Lila-Töne, die eine andere Bedeutung (Rekordnähe) haben.

### Trainer & Aktivitäten
- [x] **Aktivitäten: Zeit je Leistungszone pro Fahrt** — umgesetzt (01.09.2026, via Worker-Supervisor, 2 Runden). Detailansicht einer einzelnen Fahrt zeigt jetzt „Zeit je Zone", nutzt dieselbe `computeStreamMetrics()`/`appendZoneBars()`-Maschinerie wie die bestehenden wöchentlichen Zonen-Balken im Trainer-Tab — die Pro-Fahrt-Berechnung existierte bereits, war nur nicht verdrahtet. Abschnitt wird bei fehlenden Zonen-Daten (kein Trainingsdaten-Profil geladen) still weggelassen, wie beim „Genutztes Potential"-Chip.

### Reifendruck & Kette
- [x] **Ketten-/Verschleiß-Tracker nach Reifendruck-Muster** — umgesetzt (02.09.2026, via Worker-Supervisor, 3 Runden — 2 Review-Runden plus eine direkte Korrektur nach Erreichen der 2-Runden-Grenze). Neues, von Ausrüstung/Profil unabhängiges Werkzeug „Kette" auf der Reifendruck-Seite: einstellbare Wechsel-Schwelle (Standard 3000 km, persistiert), „Zurückgesetzt"-Button beim Kettenwechsel, Hinweis-Banner nach dem exakten Muster von `renderTirePressureBanner()` (session-only Dismiss, Klick navigiert zu Reifendruck, „×" mit `stopPropagation()`). Runde-1-Review fand einen echten Blocker: die Kilometer-Summe kam aus `state.activities`, Stravas hartcodiertem 8-Aktivitäten-Fenster — Verschleiß konnte praktisch nie sinnvoll wachsen und ein Reset brach das Tracking dauerhaft. Runde 2 behob das durch Umstellung auf den persistenten, mit der Zeit wachsenden Aktivitäten-Cache (`loadAktivitaetenListCache()`). Runde-2-Review fand einen zweiten, subtileren Bug in genau diesem Fix: der Cache ist nicht anhängend-only — weiteres Laden auf der Aktivitäten-Seite kann ÄLTERE Aktivitäten nachträglich in denselben Cache einfügen, wodurch ein distanzbasierter Reset-Basiswert (`resetAtKm`) rückwirkend aufgebläht werden konnte, ausgelöst durch eine völlig unabhängige Aktion auf einer anderen Seite. Da Runde 2 die Worker-Supervisor-Obergrenze war und der Fix selbst eindeutig war (keine Entscheidung, die nur der Nutzer treffen kann), wurde er direkt umgesetzt: der Reset-Basiswert ist jetzt ein Zeitstempel (`resetAtMs`) statt ein Distanzwert — Verschleiß seit Reset wird durch Filtern des Aktivitäten-Caches auf Einträge ab diesem Zeitpunkt berechnet, wodurch nachträglich eingefügte ältere Fahrten das Ergebnis konstruktionsbedingt nicht mehr verfälschen können. Gleichzeitig: Reifendruck-Seite spürbar verdichtet — die bisherige leere „Reifendruck"-Kopfkarte entfällt zugunsten einer schlanken Überschrift, Vorder-/Hinterrad-Karten stehen auf breiten Bildschirmen nebeneinander statt volle Breite gestapelt (klappt bei 375px weiterhin auf eine Spalte), ohne Änderung an `.card` selbst oder der Reifendruck-Rechenlogik.
- [x] **Reifendruck: "Aufgepumpt auf" verschwindet nach erster Baseline** — umgesetzt (01.09.2026, via Worker-Supervisor, 2 Runden). Sobald ein Rad einmal aufgepumpt wurde, verschwindet das anfängliche "Aufgepumpt auf"-Feld (redundant zu "Nachmessen & neu aufpumpen"). "Nachmessen & neu aufpumpen" funktioniert jetzt auch mit leerem "Gemessen"-Feld — setzt dann nur die Baseline neu, ohne Verlustraten-Lernen (vorher: kompletter No-Op bei leerem Feld). Logik in `applyNachmessenUpdate()` extrahiert und getestet. Runde 1 des Reviews fand einen echten Bug (DOM-Präsenz-Marker für den Karten-Rebuild-Check hing an einer jetzt bedingt gerenderten Input-ID, führte zu Rebuild+Fokusverlust bei jedem Settings-Refresh sobald eine Baseline existierte) — behoben durch eine unbedingte Karten-ID, Runde 2 freigegeben.

### Einstellungen & Daten laden
- [x] **Auto-Load granular einstellbar** — umgesetzt (28.08.2026). Der einzelne `autoLoadOnVisit`-Schalter ist jetzt drei unabhängige Toggles in den Einstellungen: "Aktivitäten & Route", "Markierte Segmente", "Trainingsdaten" (`autoLoadActivities`/`autoLoadSegments`/`autoLoadTraining`, migriert automatisch vom alten Schalter, falls gesetzt). Jede Kategorie hat ihre eigene manuelle Lade-Möglichkeit statt einer einzigen Karte für alles: Strecke-Seite zeigt `renderManualLoadCard()` nur noch, wenn Aktivitäten fehlen; Meine-Segmente zeigt einen eigenen "Segmente laden"-Button statt fälschlich "keine Segmente gefunden"; Trainingsdaten nutzt die bereits bestehende Profil-Lade-Karte unverändert weiter. Wichtiger Bug in der ersten Implementierung selbst gefunden und behoben: die manuellen Lade-Buttons riefen ursprünglich dieselbe flag-geprüfte Funktion wie der Auto-Load-Ablauf auf — bei deaktivierter Kategorie hätte ein Klick auf "Daten laden" dadurch schlicht nichts getan. Manuelle Trigger (`loadActivitiesStep()`/`loadSegmentsStep()`/neues `reloadAllData()` fürs Footnote-"Neu laden") laden jetzt bedingungslos, unabhängig von den Auto-Load-Einstellungen.

### Changelog-Tab
- [x] **Changelog nach Kategorien gliedern** — umgesetzt (28.08.2026). `CHANGELOG`-Einträge sind jetzt pro Datum in `features`/`fixes`/`improvements` (Arbeitstitel "Verbesserungen") aufgeteilt statt einer flachen `items`-Liste; `renderChangelogPage()` zeigt pro Datum nur die Kategorien mit tatsächlichen Einträgen (leere Kategorien fallen ganz weg, keine leere Sektion). Beide offenen Fragen entschieden: rückwirkend für alle bisherigen Einträge einsortiert (nicht nur ab jetzt), und leere Kategorien komplett ausgeblendet. Neue Kategorie-Überschrift nutzt das bereits etablierte kleine Uppercase-Label-Muster (`.changelog-category-label`, gleiche Rezeptur wie `.stat-chip .l`/`.metric-card .ml`). Changelog-Inhalt gleichzeitig aktualisiert: zwei neue Einträge für die heutigen Fixes/Features (Trainingsdaten-Auto-Load-Lücke behoben, granulare Auto-Load-Einstellung) ergänzt.
- [x] **Changelog-Tab** — umgesetzt (27.08.2026). Neuer Nav-Eintrag "Änderungen" (`renderChangelogPage`, `CHANGELOG`-Konstante) listet die letzten Updates in Klartext auf, nach Datum gruppiert. Offene Fragen entschieden: manuell gepflegt statt aus Commits generiert (Commit-Messages beschreiben die Implementierung, nicht was ein Fahrer tatsächlich merkt — z. B. "Cleanup phase 6" wäre für Nutzer bedeutungslos), zeigt aktuell die letzten paar Tage. Kleiner grüner Punkt im Nav-Item markiert ungelesene Einträge (Vergleich gegen `localStorage['velocast_changelog_seen']`), verschwindet beim Öffnen der Seite.
